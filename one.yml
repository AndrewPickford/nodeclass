alpha:
  dict:
    a: 1
    b: 2
  list:
    - 1
    - 2
  ref:
    a: ${alpha:dict:a}
    c: ${alpha:dict:c}
    x: ${alpha:dict}
    y: ${alpha:list}
    z: ${alpha:dict:a}-${alpha:dict:b}-${alpha:dict:c}
